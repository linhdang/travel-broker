package mams;

agent TravelAgent {
    module System system;
    module Console console;
    
    types plan_request {
        // departure, arrival, accomodationType, from, to, plannerId

        // formula plan_request_action(string, string, string, long, long, string);
        formula plan_request_action(string, string);
    }
    
    rule +!init() {
    }

}